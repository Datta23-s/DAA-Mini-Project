{
  "name": "connectiq-unified",
  "version": "1.0.0",
  "description": "Unified build script for Render deployment",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "render-postbuild": "cp -R frontend/dist backend/static_frontend"
  }
}
